// Base da API. Vazio = mesma origem (docker-compose local, nginx faz proxy de /api).
// No Cloudflare Pages, aponte para o subdominio da API, ex.: "https://api-spotify.alexarnoni.com"
window.API_BASE = "";
